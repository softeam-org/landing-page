/*const API_URL = process.env.NEXT_PUBLIC_API_URL;

export async function FeatureFlagsService(

): Promise<void> {
  const response = await fetch(API_URL + "/flags/project/1")

  console.log(response);

  if (!response.ok) {
    throw new Error("Erro ao enviar inscrição");
  }
}*/
